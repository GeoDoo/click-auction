# Click Auction - Environment Variables
# Copy this to .env and customize

# Optional: PIN to protect /host route (leave empty for open access)
HOST_PIN=

# Optional: Redis for persistent scores (leave empty for local file storage)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Server port (usually set automatically by hosting platform)
# PORT=3000
